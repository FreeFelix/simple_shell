#include "main.h"
/**
 * ext -exit function used to terminate process
 * @av: passed args
 *
 * Return: Always(success)
 */
int ext(char **av)
{
if (av & av[1])
{
return (atoi(args[1]));
}
return (0);
}
